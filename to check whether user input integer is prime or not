#include <iostream.h>
#include <conio.h>

void main() {

	clrscr();

	int num;
	cout<<"  Enter The Number";
	cin>>num;

	for(int i=2; i<=num-1; i++)
	{	if (num%i==0)
			break;
	}

	{	if (i==num)
			cout<<"Prime no.";
		else
			cout<<"Not Prime";
	}
	getch();
